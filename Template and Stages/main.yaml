trigger: none 

pool:  
  vmImage: 'ubuntu-latest'  

variables:
- template: var.yaml

stages:
- template: build.yaml
 
- template: stage.yaml
  parameters:
    stage_name: ${{ variables.stage_name }}
    dependsonStage: ${{ variables.dependsonStage }}
    shiv: ${{ variables.shiv }}

- template: stage.yaml
  parameters:
    stage_name: ${{ variables.stage_name }}
    dependsonStage: ${{ variables.dependsonStage }}
    shiv: ${{ variables.shiv }}

# extends:
#    template: "YamlTemplates/Stage/Entrypoint.yaml"  
#    parameters:  
#        RunChangeLogic: 'true'